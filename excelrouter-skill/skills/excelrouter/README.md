<!--
技能包内的说明文件。**注意：SkillHub 详情页「概述」那一栏渲染的不是它，是 SKILL.md**
（2026-09-23 在干净浏览器里实测确认：「概述」里出现的是 SKILL.md 的「给 AI 助手的
执行手册」等章节，本文件独有的小标题一个都没有）。

之前有过一次误判：看到本包 README.md 返回 404、而另一个技能有 README 且「概述」
正常，就推断「概述 = README.md」。那是巧合——真正的原因是那份文档要几秒才加载完，
中途失败就显示「无法加载文档 / Failed to fetch」，跟有没有 README 无关。
**要改详情页给人看的内容，请改 SKILL.md 的正文上半部分。**

本文件仍然有用：GitHub 上浏览 skills/excelrouter/ 子目录时显示的是它，ClawHub 一侧
也可能取它。内容与 SKILL.md 上半部分重合是有意的，改其中一处时顺手同步另一处。
-->

# ExcelRouter · 表格拆分 & PDF 加密分发

## 一句话

一张几千行的总表，按部门 / 区域 / 工号 / 姓名拆成几十个文件发给不同的人——
跟 AI 说一句「把这张工资表按人拆成一人一份」就拆好了，**原表格式原样保留**。

手工做这件事是「筛选 → 复制 → 新建工作簿 → 粘贴 → 调格式 → 另存」重复几十遍，
一个下午；写脚本则要自己处理表头识别、样式复制、跨文件合并。这个技能把这两件事都省了。

## 三步，说人话就行

```
① 看看表          ② 说怎么拆           ③ 拿走文件
   有哪些字段   →    按部门 / 按人    →    AI 把输出路径和份数报给你
   有哪些取值        只拆哪几个值
```

直接说给 AI 听：

- 「把这张工资表按人拆成一人一份」
- 「这个文件夹里所有网点表按网格拆一遍，同一个人的合并成一份」
- 「按部门拆成每个部门一个文件，只拆销售部和技术部」
- 「这批 PDF 按网格设置不同密码再分发」
- 「先看看这张表有哪些字段可以拆」

## 和通用 Excel 读写技能的区别

| | 通用 Excel 技能 | ExcelRouter |
|---|---|---|
| 表头不在第一行（有大标题、多层合并表头） | 经常读错 | 自动识别 |
| 原表的字体 / 颜色 / 边框 / 列宽 | 基本丢失 | 完整保留，拆出来不用再修 |
| 一整个文件夹的表 | 一张一张来 | 一次拆完；同一个人散在多个表里会自动并成一份 |
| 拆完还要在每组里再按人拆 | 自己写循环 | 一条命令做完两级 |
| 一份 PDF 发 100 个人、100 个不同密码 | 做不了 | AES-256 + 每人专属可溯源水印 |

## 它不做这些

写在前面，免得你装完才发现不合用：

- ❌ **不做表头扁平化 / 多层表头降维** —— 定位是「原样保留」表头，不是改造表头
- ❌ **不做把多个表合并成一个总表**的通用合并（只在拆分时支持「同一取值跨文件合并」）
- ❌ **不做数据透视、清洗、统计分析、图表**
- ⚠️ **数据区的合并单元格不保留**（表头区的保留）—— 拆完每行各自取值，原来几行共用的格会空
- ⚠️ **`.xls` 旧格式**会先转成 xlsx，格式在这一步就丢了；在意格式请先另存为 `.xlsx`
- ⚠️ **源表公式若从未被 Excel 真正算过**（程序生成的表），读到的是空白；工具会提前警告你

## 数据不出本机

全程在你自己的机器上处理，**不联网、不上传任何数据到第三方**，断网也能用。
工资表、花名册、客户清单、成绩单这些东西，本来就不该传到别人的服务器上——
这是它和「在线拆分网站」的根本区别。

代码 MIT 开源可查；SkillHub 的两套安全扫描（keen / sanbu）结论均为**「安全，无风险」**。

## 装上之后

```bash
pip install -r requirements.txt        # 只拆 Excel：openpyxl、pandas、xlrd
pip install -r requirements-pdf.txt    # 要做 PDF 加密分发时再装这 3 个
```

不用记命令，直接跟 AI 说人话。缺什么依赖脚本会用人话告诉你。

**大表能拆吗？** 没有硬性上限，约 1.5 万单元格/秒：1 万行 × 15 列约 11 秒，
5 万行约 49 秒。跑的时候有进度心跳，不会让你对着静默的界面猜是不是卡死了。

## 也有图形界面版（同一套内核）

不想用命令行、或者要给不会装 Python 的同事用，有免费的 Windows 桌面版：
选表格 → 选字段 → 点开始，解压双击即用，MIT 开源，数据同样不出本机。

- 直接下载（点开即下，免安装免注册，约 52 MB）：
  <https://github.com/MarsandSea/ExcelRouter/releases/latest/download/ExcelRouter-Windows.zip>
- 介绍与使用说明：<https://marsandsea.github.io/ExcelRouter/>
- 源码与反馈：<https://github.com/MarsandSea/ExcelRouter>
